# <MODULE> 行为规约(spec.md)

**版本**: v1(YYYY-MM-DD)
**模块**: `path/to/module.py` 或工人 / 业务编排所在位置
**类型**: 同步业务函数 / 异步 task 工人 / 数据模型 / 工具集
**对应 ADR**:
- `docs/adr/NNNN-<决策名>.md`(简短说明它为什么影响本模块)
- ...

> 复制本模板新建 spec 时:
> 1. 文件名固定 `spec.md`, 住在 `test/<module>/spec.md`
> 2. 删掉本框内所有提示文字
> 3. 按下面三段式填写, 不需要的小节删掉
> 4. 每次改动加 §三 修订历史一条

---

## 一、整理后的要点

> 这一段是 Claude 整理后的当下真相, 用户审过的版本。
> 开发 / 测试照这一段做。读者一眼能看到模块全貌。

### 1. 模块定位

(这个模块/工人是干什么的, 1-3 句话)

### 2. 入口契约

**触发**: 谁调它(MCP 工具 / 另一个工人派 task / 定时轮询 / ...)

**入参 / 抢锁** (如适用):

```
入参形状或抢锁 SQL
```

**输入**: 来自哪里的什么数据

**输出**:
- 成功: ...
- 失败: ...(分支)

### 3. 主流程 / 分支

> 这是模块的核心。
> 用业务语言列步骤, 关键判断列分支, 数据流标清楚。
> 必要时用 ASCII 图。

#### 分支 A: <情况描述>

1. ...
2. ...

#### 分支 B: <情况描述>

...

### 4. 统一收尾 / 共同后置 (如适用)

所有分支必经的收尾步骤:

1. ...
2. ...

### 5. 端口 / 资源规则 (如适用)

(模块涉及的资源约束、命名规则、分配算法等)

### 6. 状态字段语义 (如适用)

`<table>.<field>` 表示什么, 由谁写, 何时更新。

### 7. 失败处理

| 场景 | 终态 | 错误码 |
|---|---|---|
| ... | failed / pending_retry / circuit_broken | `xxx` |

### 8. 不做的事

明确列出**不在本模块职责范围**的事(避免实现者越界):

- 不做 X(那是 <其他模块> 的事)
- 不做 Y
- 唯一例外(如有): ...

### 9. 不变量

完成时必须满足的硬约束:

- ...
- ...

### 10. 边界情况

| 情况 | 期望行为 |
|---|---|
| ... | ... |

---

## 二、工具清单

> 实现这个模块需要的工具, 按层分类。
> Claude 必须 spec 阶段就反推完整, 用户审过才开"造工具"任务单(见 [[CLAUDE.md §2.5]])。

### A. <某个工具箱>方法

- `method_a(...)` — 一句话用途
- `method_b(...)` — ...

### B. <某个底层 / 包>底层函数

- `func_x(...)` — ...
- `func_y(...)` — ...

### C. <通用工具箱>

- `tool_a(...)` — ...

### D. 本模块私有编排

按 [[feedback-工具编排发现式抽取]] 原则: 私有编排住在本模块 `.py` 内部, 命名按实现者方便,
spec 不强制锁死方法名。

只有跨多个模块重复出现的编排, 才往上抽到工具箱。

---

## 三、修订历史

- vN YYYY-MM-DD: <本次修订一句话总结>
  - 改了 §X(主要改动)
  - 改了 §Y
  - 工具清单加了 / 减了什么

- v1 YYYY-MM-DD: 初版。
