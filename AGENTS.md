# AGENTS.md

本项目的共同协作约束集中维护在 [CLAUDE.md](CLAUDE.md)。
本地/临时补充约束维护在 [CLAUDE.local.md](CLAUDE.local.md)。

所有 agent 开工前先阅读 `CLAUDE.md` 和 `CLAUDE.local.md`，尤其是：

- 核心约束
- 三层架构
- MCP 工具层必须和后端业务解耦
- 日志契约
- 测试契约
- 工作流

不要在本文件重复复制规则；通用规则只在 `CLAUDE.md` 维护，本地补充只在 `CLAUDE.local.md` 维护，避免多处漂移。
