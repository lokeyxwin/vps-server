"""Proxy 领域包：从 VPS + IP 自动部署代理出口（占位）。

未来内容：
    atom.py     ← get_used_ports / config 注入等原子操作 + 错误类
    manager.py  ← ProxyManager 类，封装部署一条代理的完整生命周期

业务函数在 services/proxy_*.py，由后台任务而非用户 CLI 触发。
"""
pass
