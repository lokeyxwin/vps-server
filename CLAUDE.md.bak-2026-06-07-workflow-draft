# CLAUDE.md draft

版本: draft-2026-06-07-python-backend-universal

本文件只放跨项目长期有效的 Claude 进场规则、文件管理系统和协作节奏。  
默认项目形态按 **Python 后端服务** 理解: 会涉及业务流程、数据流、同步/异步边界、
业务决策判断、数据库结构、状态流、外部系统、测试规约和任务拆解。

详细模板、示例、沉淀方法和一次性工作流, 应拆到按需加载的 skill 或项目文档,
不要默认塞进所有会话。

---

## 0. 边界: 哪些内容能写进本文件

`CLAUDE.md` 是跨项目通用规则。换一个后端项目仍然能成立的内容, 才能写进这里。

| 文件/目录 | 负责什么 |
|---|---|
| `CLAUDE.md` | 跨项目通用协作规则、文件管理系统、Python 后端通用命名/拆解方法 |
| `CLAUDE.local.md` | 当前项目专属业务、技术栈、实现禁区、本地命令和特殊约束 |
| `docs/adr/` | 已拍板的架构事实、业务流程、数据流、架构图、决策原因 |
| `test/` | 行为规约和行为测试, 是验收标准的金标准 |
| `issue/` | 未拍板想法、临时痛点、待讨论问题 |
| `task/` | 已拍板、可交给实现窗口直接开干的任务资料包 |

判断口诀:

```text
换项目还能用                         -> CLAUDE.md
依赖当前业务对象/技术栈/真实路径       -> CLAUDE.local.md
解释为什么这么定                     -> docs/adr/
描述应该达到什么效果                 -> test/<group>/spec.md
还没拍板                             -> issue/
已经拍板, 要去实现                   -> task/
```

硬规则:

- `CLAUDE.md` 不写当前项目真实业务名、软件名、端口号、表名、CLI 命令、worker 名。
- 通用示例统一使用占位符, 不使用项目专属签名。
- 项目专属规则必须下沉到 `CLAUDE.local.md`、ADR、spec 或 task。

---

## 1. 占位符规范

跨项目规则里统一用占位符表达业务对象和代码位置。

| 占位符 | 含义 |
|---|---|
| `<project_domain>` | 当前项目领域, 如某类资产管理、订单系统、账号系统 |
| `<entity>` | 业务实体, 如 account/order/node/job 等 |
| `<resource>` | 外部或内部资源, 如 remote host、API token、file、queue item |
| `<flow_name>` | 业务流程名 |
| `<worker>` | 负责一段业务判断或状态推进的工人 |
| `<service>` | 对外或内部服务边界 |
| `<manager>` | 封装某类资源生命周期或多步工具编排的类 |
| `<repository>` | 数据访问封装或持久化边界 |
| `<record>` | ORM record / database row 对应对象 |
| `<table_name>` | 数据库表名 |
| `<task_type>` | 后台任务类型 |
| `<state>` | 状态机状态 |
| `<external_system>` | 外部系统, 如第三方 API、远端机器、消息队列、支付网关 |
| `<decision_name>` | 某个已拍板架构决策 |

Python 命名示例也必须通用:

| 中文动作 | Python 占位命名 |
|---|---|
| 查重 | `_find_existing_record` |
| 探测外部资源 | `_probe_external_resource` |
| 采集上下文 | `_collect_resource_context` |
| 入库并派任务 | `_persist_entity_and_enqueue_task` |
| 推进状态 | `_transition_task_state` |
| 处理失败分支 | `_handle_processing_failure` |
| 构建返回值 | `_build_response_payload` |

禁止在通用规则里出现类似 `_find_existing_<real_business_object>`、
`<real_table_name>`、`<real_worker_name>` 这种当前项目签名。

---

## 2. 会话启动时先读什么

新会话进入项目后, 先建立当前项目心智地图, 再判断或实现。

默认读取顺序:

1. 读 `CLAUDE.local.md`  
   了解当前项目业务约束、技术栈约束、实现禁区、本地命令和专属规则。

2. 读 `docs/adr/README.md` 和 `docs/adr/*.md`  
   ADR 是已拍板架构事实源。按编号顺序读, 理解当前业务流程、数据流、架构图、
   分支判断、状态流、数据库结构和重要决策原因。

3. 看 `task/` 文件名  
   通过文件名前缀了解任务状态: `waiting` / `doing` / `done`。  
   不默认读所有 task 内容。只有用户指定任务、请求实现, 或本轮工作明显依赖某个
   task 时, 才读对应任务单。

4. 知道 `issue/` 的用途, 但不要默认读内容  
   `issue/` 存放未拍板想法、临时痛点和待讨论问题。  
   只有用户明确要求回看某个 issue, 或当前讨论要续上未拍板问题时, 才读相关 issue。

5. 读相关 `test/` 资料  
   先读 `test/README.md` 理解本项目测试分组。  
   如果本轮涉及某个 `<group>` / `<worker>` / `<flow_name>` / `<entity>`,
   再读对应 `test/<group>/spec.md` 和必要测试文件。

实现窗口最小读取集:

```text
CLAUDE.md
CLAUDE.local.md
docs/adr/README.md
docs/adr/*.md
task/<state>_NN_<slug>.md
task 中引用的 test/<group>/spec.md
task 中点名的目标代码文件和测试文件
```

---

## 3. 最小文件骨架

推荐所有 Python 后端项目保持这套骨架:

```text
CLAUDE.md
CLAUDE.local.md
docs/
└── adr/
    ├── README.md
    └── NNNN-<decision-name>.md
issue/
└── YYYY-MM-DD-<topic>.md
task/
└── <state>_NN_<slug>.md
test/
├── README.md
├── <group>/
│   ├── spec.md
│   └── TC-*.py
└── _data_structures/
```

目录职责:

| 位置 | 职责 | 默认读取策略 |
|---|---|---|
| `docs/adr/` | 架构事实、业务流程、数据流、图、决策原因 | 启动时读取全部 ADR |
| `task/` | 已拍板实现资料包 | 启动时只看文件名; 实现时读目标任务 |
| `issue/` | 未拍板讨论和临时问题 | 只知道用途; 不默认读内容 |
| `test/` | 行为规约和行为测试 | 按本轮相关模块读取 |
| `CLAUDE.local.md` | 当前项目专属约束 | 启动时读取 |

---

## 4. issue -> ADR/spec/task 的沉淀链路

用户口述经常是散点的。Claude 要负责把散点整理成能接力的新窗口资料。

处理顺序:

```text
用户口述
  -> 先判断是否已拍板
  -> 未拍板: 进 issue/
  -> 已拍板的架构/流程/数据/状态/边界: 进 docs/adr/
  -> 已拍板的验收标准: 进 test/<group>/spec.md
  -> 已拍板且可实现: 拆成 task/waiting_NN_<slug>.md
```

落文件前要先判断归属:

- 这是业务事实、架构事实、数据流、同步/异步边界、状态机、数据库结构吗?
  是的话优先考虑 ADR。
- 这是用户对正确行为的判断标准吗?
  是的话优先考虑 `test/<group>/spec.md`。
- 这是还没定的想法或痛点吗?
  是的话放 `issue/`。
- 这是可以实现的具体工作吗?
  是的话进入 `task/`。

冲突规则:

- 新口述和既有 ADR/spec 冲突时, 先列冲突点问用户, 不直接覆盖。
- 新口述像补充但归属不清时, 先问归属, 不自作主张建新文件。
- 用户拍板后, 再把事实下沉到对应文件。

---

## 5. ADR 是架构事实源

ADR 不只是记录"为什么这么定", 还要让新窗口读完后知道当前项目怎么运转。

ADR 里优先维护:

1. 用户事实语录  
   保留用户对业务事实、流程判断、边界条件的关键原话。

2. 当前业务流程图  
   描述外部输入进来后, 哪个 `<worker>` / `<service>` 先处理, 谁后处理,
   成功/失败怎么走, 什么时候返回用户。

3. 当前架构图  
   描述入口、服务层、worker、manager、repository、数据库、外部系统之间的关系。

4. 数据流图  
   描述数据从入口、业务层、数据库、队列、外部系统之间怎么流转,
   谁读、谁写、谁是事实源。

5. 同步/异步边界  
   描述哪些流程同步返回结果, 哪些流程创建 `<task_type>` 后异步推进,
   用户或 agent 如何查询进度。

6. 分支判断地图  
   描述 if/else 条件、判断标准、失败分类、重试、熔断、回退规则。

7. 数据库/状态结构  
   描述关键 `<table_name>`、`<record>`、字段大类、状态机和状态迁移。

8. 当前版本字段  
   流程图、架构图、数据流图和状态流必须反映当前已拍板版本。

图可以用 Mermaid 或 ASCII。通用占位示例:

```mermaid
flowchart TD
    A["External request"] --> B["<entrypoint>"]
    B --> C["<service>"]
    C --> D{"Business decision"}
    D -->|success| E["<repository>: write <record>"]
    D -->|needs async work| F["enqueue <task_type>"]
    D -->|failure| G["return typed failure"]
```

处理规则:

- 用户描述业务流程时, 先抽取事实, 再判断是否更新 ADR。
- 如果新描述和 ADR 现有事实冲突, 先列冲突点问用户, 不直接覆盖。
- 如果是在查漏补缺, Claude 可以列建议, 但必须问用户要不要采纳。
- ADR 不写实现任务清单; 任务清单放 `task/`。
- ADR 可以引用 spec 和 task, 但不复制整段实现细节。

---

## 6. test 是行为金标准

用户描述业务流程时, 往往天然包含测试用例的金标准。Claude 要主动提取:

- 输入是什么。
- 前置状态是什么。
- 期望输出是什么。
- 哪些副作用必须发生。
- 哪些事情不应该发生。
- 失败分支应该怎么返回、落库或进入状态机。
- 同步/异步流程分别如何验收。

这些行为标准写进对应 `test/<group>/spec.md`。

`test/` 的组织维度按项目决定:

| 项目形态 | 可选组织方式 |
|---|---|
| worker 架构 | 按 `<worker>` 分 |
| 流程型项目 | 按 `<flow_name>` 分 |
| 模块型项目 | 按 `<module>` 分 |
| 数据结构/schema | 单独放 `_data_structures/` 或 `db/` |

最小骨架:

```text
test/
├── README.md
├── <group>/
│   ├── spec.md
│   └── TC-*.py
└── _data_structures/
```

规则:

- `spec.md` 是行为验收标准唯一真相源。
- 测试文件不要复制整段 spec, 只引用对应 spec。
- 当用户描述带出新测试标准时, 先判断归属哪个 spec, 再处理冲突,
  再问用户是否更新。
- 如果实现和 spec 有偏差, 优先认为实现需要修; 如果偏差可能是合理设计变更,
  先交给用户拍板。

---

## 7. task 是实现资料包

`task/` 只放已经拍板、可以交给实现窗口直接开干的工作。

文件名:

```text
task/waiting_NN_<slug>.md
task/doing_NN_<slug>.md
task/done_NN_<slug>.md
```

状态含义:

| 前缀 | 含义 |
|---|---|
| `waiting` | 已拍板, 等待实现 |
| `doing` | 当前窗口正在实现 |
| `done` | 已实现并完成对应验证 |

任务单必须让新窗口读完就能开干, 至少包含:

- 目标行为。
- 相关 ADR。
- 相关 `test/<group>/spec.md`。
- 要改哪些文件。
- 实现轮廓。
- 业务判断/状态迁移要点。
- 要新增或修改哪些测试。
- 必须跑的测试命令。
- 验收成功的判断准则。
- 非目标和禁止事项。

任务单不复制整段 ADR/spec, 只引用。  
实现窗口按任务单和相关事实源实现, 不需要默认理解整套文件系统维护方法。

完成任务时:

- 将 `waiting` 或 `doing` 前缀改为 `done`。
- 在任务单末尾追加简短完成记录: 改动文件、测试命令、测试结果、未覆盖风险。
- 不改写原始目标行为; 如果目标行为变了, 应先更新 spec/ADR 或新开任务。

---

## 8. issue 只放未拍板内容

`issue/` 用于保存还没拍板的想法、问题、痛点和备选方案。

规则:

- 启动会话时不要主动读取 issue 内容。
- 用户明确说要回看某个 issue, 或当前讨论需要续上未拍板问题时, 才读取相关 issue。
- issue 里可以整理问题背景、候选方案、待拍板问题。
- 已经拍板的内容要沉淀到 ADR / spec / task, 不继续只留在 issue。

---

## 9. 语言分层

中文用于沟通和行为对齐。  
英文/ASCII 用于代码标识符。

| 产物 / 场景 | 语言规则 |
|---|---|
| 给用户的回复 | 中文大白话 |
| 业务流程 / 行为故事 / 用户事实语录 | 中文大白话 |
| issue / ADR / spec / task 的解释性文字 | 中文为主, 关键代码名/字段名保持英文 |
| task 的实现指引 | 中文解释 + 英文代码标识符 |
| 源代码 | 文件名、类名、函数名、变量名、字段名、API 名必须英文/ASCII |
| 代码注释 / docstring | 只描述当前代码事实, 不写讨论过程和修订历史 |

硬规则:

- 中文口语动作名不能直接变成代码函数名或方法名。
- 任务单里如果写了中文步骤, 必须给出对应英文代码标识符。
- Python 命名默认使用 `snake_case` 函数/变量、`PascalCase` 类名、
  `UPPER_SNAKE_CASE` 常量。

通用 Python 命名方向:

| 类型 | 命名倾向 |
|---|---|
| 业务入口函数 | `<verb>_<entity>` |
| 私有步骤函数 | `_<verb>_<object>` |
| 状态推进函数 | `_transition_<entity>_state` |
| 失败处理函数 | `_handle_<reason>_failure` |
| 数据读取函数 | `_find_*` / `_get_*` / `_list_*` |
| 数据写入函数 | `_create_*` / `_update_*` / `_persist_*` |
| 资源生命周期类 | `<Resource>Manager` |
| 数据访问类 | `<Entity>Repository` |
| ORM 模型 | `<Entity>Record` 或项目约定名称 |
| worker 类 | `<FlowOrEntity>Worker` |

---

## 10. 代码注释边界

代码注释和 docstring 是给实现者读代码用的, 只描述代码事实:

- 当前函数做什么。
- 输入和返回是什么。
- 关键副作用是什么。
- 不明显的实现选择是什么。

不要写:

- spec 版本史。
- 用户讨论过程。
- 上层架构争论。
- 任务单执行记录。

反例:

```python
def _find_existing_record(entity_id: str) -> dict | None:
    """Look up a record.

    spec v4:
      - removed old_status_message
      - no longer uses OLD_PENDING_STATE
    """
```

正例:

```python
def _find_existing_record(entity_id: str) -> dict | None:
    """Return the stored record summary, or None when it does not exist."""
```

历史原因写进 ADR, 行为变化写进 spec 修订历史, 实现计划写进 task,
不写进代码注释。

---

## 11. 默认工作姿势

- 先读事实源, 再判断。
- 不确定归属时先问。
- 用户散点口述时, 主动抽取流程、数据、分支、测试标准和待拍板问题。
- 冲突先列出来, 让用户拍板。
- 已拍板事实要从 issue 下沉到 ADR / spec / task。
- 不把按需 workflow 默认塞给实现会话。
- 不把项目专属名词写进通用规则。

最终目标:

```text
上一个窗口负责沟通、归类、沉淀:
  issue -> ADR -> spec -> task

新窗口负责接力实现:
  读 CLAUDE + local + ADR + 目标 task + 对应 spec
  -> 改代码
  -> 写/跑测试
  -> 标记 task done
```
